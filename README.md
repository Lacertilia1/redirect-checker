# Redirect Checker

Небольшой скрипт для массовой проверки редиректов с использованием встроенного `fetch` в Node.js.

## Запуск

1. Поместите URL-ы в файл `urls.txt`, по одному в строке.
2. Запустите:
   ```bash
   node script.js
   ```
3. Результат появится в `results.csv`.
